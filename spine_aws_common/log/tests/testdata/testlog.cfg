[TESTINFO001]
Log Level = INFO
Log Text = Info level log with message="{message}" for internalID={internalID}

[TESTDEBUG001]
Log Level = DEBUG
Log Text = Debug level log with message="{message}" for internalID={internalID}

[TESTCRITICAL001]
Log Level = CRITICAL
Log Text = Critical level log with message="{message}" for internalID={internalID}

[TESTAUDIT001]
Log Level = AUDIT
Log Text = Audit level log with message="{message}" for internalID={internalID}

[TESTMASKEDINFO001]
Log Level = INFO
Log Text = Log with message="{message}" and data that may be masked url={url} for internalID={internalID}

[TESTMASKEDAUDIT001]
Log Level = AUDIT
Log Text = Log with message="{message}" and data that may be masked url={url} for internalID={internalID}

[UTI9992]
Log Level = CRITICAL
Log Text = Crash dump occurred. See crashdump index for details with originalLogReference={originalLogReference}